[project]
name = "log-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==2.8.0",
    "pandas==3.0.3",
]

[tool.uv]
add-bounds = "exact"
exclude-newer = "7 days ago"

[dependency-groups]
dev = [
    "pytest==9.0.3",
]
